<Window x:Class="JudgeWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JudgeWPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="1000"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded"
        Unloaded="Window_Unloaded"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">
    <Window.Resources>
        <Style x:Key="DataGridContentCellCentering" TargetType="{x:Type DataGridCell}" BasedOn="{StaticResource MaterialDesignDataGridCell}">
            <Setter Property="HorizontalAlignment" Value="Center" />
        </Style>
        <Style x:Key="ProblemsHeaderTemplate" TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Border>
                            <TextBlock Text="{Binding}" Foreground="Blue"/>
                        </Border>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="FontFamily" Value="Arial"/>
            <Setter Property="FontSize" Value="14" />
        </Style>
        <Style x:Key="UsersHeaderTemplate" TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Border>
                            <TextBlock Text="{Binding}" Foreground="Red"/>
                        </Border>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="FontFamily" Value="Arial"/>
            <Setter Property="FontSize" Value="14" />
        </Style>
    </Window.Resources>
    <Grid>
        <DockPanel>
            <StackPanel
                Orientation="Horizontal"
                DockPanel.Dock="Top"
                HorizontalAlignment="Center">
                <Button Style="{DynamicResource MaterialDesignRaisedButton}"
                        x:Name="btnLoadContest"
                        Margin="0, 8, 2, 8"
                        Click="btnLoadContest_Click">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="OpenInNew" Margin="0, 0, 2, 0"/>
                        <TextBlock Text="Load Contest" />
                    </StackPanel>
                </Button>
                <Button Style="{DynamicResource MaterialDesignRaisedButton}"
                        x:Name="btnSaveContest"
                        Margin="2, 8, 2, 8"
                        Click="btnSaveContest_Click">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ContentSave" Margin="0, 0, 2, 0"/>
                        <TextBlock Text="Save Contest"/>
                    </StackPanel>
                </Button>
                <Button x:Name="btnStartGrading"
                        Click="btnStartGrading_Click"
                        Margin="2, 8, 2, 8"
                        Style="{DynamicResource MaterialDesignRaisedButton}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Marker" Margin="0, 0, 2, 0"/>
                        <TextBlock Text="Start grading" />
                    </StackPanel>
                </Button>
                <Button Style="{DynamicResource MaterialDesignRaisedButton}"
                        Margin="2, 8, 2, 8"
                        x:Name="btnExportExcel"
                        Click="btnExportExcel_Click"
                        >
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="MicrosoftExcel" Margin="0, 0, 2, 0"/>
                        <TextBlock Text="Export excel" />
                    </StackPanel>
                </Button>
                <Button Style="{DynamicResource MaterialDesignRaisedButton}"
                        Margin="2, 8, 2, 8"
                        x:Name="btnSettings"
                        >
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Settings" Margin="0, 0, 2, 0"/>
                        <TextBlock Text="Settings" />
                    </StackPanel>
                </Button>
                <Button Style="{DynamicResource MaterialDesignRaisedButton}"
                        Margin="2, 8, 2, 8"
                        x:Name="btnHelp"
                        >
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Help" Margin="0, 0, 2, 0"/>
                        <TextBlock Text="Help" />
                    </StackPanel>
                </Button>
            </StackPanel>
            <DockPanel>
                <TabControl>
                    <TabItem>
                        <TabItem.Header>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Scoreboard" Margin="0, 0, 4, 0" VerticalAlignment="Center"/>
                                <TextBlock Text="Scoreboard" FontSize="14" FontFamily="Arial"/>
                            </StackPanel>
                        </TabItem.Header>
                        <DataGrid x:Name="scoreBoard"
                                  CellStyle="{StaticResource DataGridContentCellCentering}"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  CanUserDeleteRows="False"
                                  CanUserAddRows="False"
                                  CanUserResizeRows="False"
                                  CanUserResizeColumns="False"
                                  CanUserReorderColumns="False"
                                  CanUserSortColumns="False"
                                  ColumnWidth="*"
                                  SelectionUnit="Cell"
                                  SelectionMode="Single"
                                  SelectionChanged="scoreBoard_SelectionChanged"
                            >
                        </DataGrid>
                    </TabItem>
                    <TabItem>
                        <TabItem.Header>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ListStatus" Margin="0, 0, 4, 0" VerticalAlignment="Center"/>
                                <TextBlock Text="Problems setting" FontFamily="Arial" FontSize="14"/>
                            </StackPanel>
                        </TabItem.Header>
                        <TabControl TabStripPlacement="Left">
                            <TabItem>
                                <TabItem.Header>
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="ListStatus" Margin="0, 0, 4, 0" VerticalAlignment="Center"/>
                                        <TextBlock Text="Problems setting" FontFamily="Arial" FontSize="14"/>
                                    </StackPanel>
                                </TabItem.Header>
                            </TabItem>
                            <TabItem>
                                <TabItem.Header>
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="ListStatus" Margin="0, 0, 4, 0" VerticalAlignment="Center"/>
                                        <TextBlock Text="SumAB" FontFamily="Arial" FontSize="14"/>
                                    </StackPanel>
                                </TabItem.Header>
                            </TabItem>
                        </TabControl>
                    </TabItem>
                </TabControl>
            </DockPanel>
        </DockPanel>
    </Grid>
</Window>
