#include <stdio.h>
void nhap(int a[],int n)
{
	for (int i=0;i<n;i++)
		scanf("%d",&a[i]);
}
int ktt(int a[],int n)
{
	int 
	for (int i=0; i+2<n;i+2)
		if (a[i]%2==0 ) return 0;
		return 1;
		}
int kts(int a[],int n)
{
	int 
	for (int i=1; i+2<n;i+2)
		if (a[i]%2==0 ) return 0;
		return 1;
}

